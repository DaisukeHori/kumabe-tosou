import type { PageTextSlot } from "../types";

// ---------------------------------------------------------------------------
// privacy (0, route: "/privacy") — 後続 Wave でスロットを追加する (現時点は空)
// ---------------------------------------------------------------------------
export const PRIVACY_TEXT_SLOTS: readonly PageTextSlot[] = [];
