import type { PageTextSlot } from "../types";

// ---------------------------------------------------------------------------
// voices (0, route: "/voices") — 後続 Wave でスロットを追加する (現時点は空)
// ---------------------------------------------------------------------------
export const VOICES_TEXT_SLOTS: readonly PageTextSlot[] = [];
