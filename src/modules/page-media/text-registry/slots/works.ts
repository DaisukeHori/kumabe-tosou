import type { PageTextSlot } from "../types";

// ---------------------------------------------------------------------------
// works (0, route: "/works") — 後続 Wave でスロットを追加する (現時点は空)
// ---------------------------------------------------------------------------
export const WORKS_TEXT_SLOTS: readonly PageTextSlot[] = [];
