# kumabe-tosou — GitHub Pages (静的公開用ブランチ)

このブランチ (`gh-pages`) は GitHub Pages 配信専用です。開発は `main` ブランチ（Next.js アプリ）で行っており、本ブランチはその影響を受けません。

- `/` … 公開サイトのモックアップ（`main` ブランチ `legacy/` の静的HTML版をそのまま配信）
- `/admin-mockup/` … 管理画面リデザインのモックアップ（スタンドアロンHTML、閲覧専用）

いずれも実データには接続していない画面設計モックアップです。
